{
  "devDependencies": {
    "eslint": "^8.53.0",
    "prettier": "^3.0.3"
  },
  "scripts": {
        "lint": "prettier --write \"{server,client}/**/*.ts\" && eslint \"{server,client}/**/*.ts\" --fix --max-warnings 0"
  }
}
